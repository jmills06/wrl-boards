__pycache__/
*.py[cod]

# atomic-write temporaries; never committed
data/latest/*.tmp

# local discovery output
discovery*.txt

# Board screenshots taken while iterating on a design. They are working
# files, not board assets: the boards render from data/latest/*.json.
*.png
